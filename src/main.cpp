#include "backup.hpp"
#include <iostream>

int main(int argc, char** argv) {
    // Placeholder main to satisfy linker for now (will be implemented in GREEN)
    // For initial RED, we can return non-zero so the test fails when invoking CLI.
    std::cerr << "CLI not implemented yet" << std::endl;
    return 42;
}
